# 📝 일일 코딩 테스트 기록부

매일의 학습 내용을 확인하기 위한 작은 문제들과 풀이를 기록합니다.

---

## 🗓️ Day 1: Cargo 및 기본 구조
### 문제 1: Cargo로 새 프로젝트 생성하고 실행하기
**과제**: `study_01`이라는 이름의 새로운 Cargo 프로젝트를 생성하고, `main.rs`의 내용을 수정하여 본인의 이름이나 "Hello Rust Master!"를 출력한 뒤 실행해 보세요.

**수행 결과**:
- [ ] 프로젝트 생성 완료 (`cargo new`)
- [ ] 코드 수정 완료
- [x] 실행 성공 (`cargo run`)

---

## 🗓️ Day 2: 변수와 가변성
### 문제 2: 변수의 가변성 체험하기
**과제**: `src/main.rs`의 내용을 모두 지우고, 다음 시나리오를 코드로 작성해 보세요.
1. `score`라는 변수에 100을 담습니다.
2. `score`를 출력합니다.
3. `score`에 200을 다시 대입합니다. (이때 에러가 발생한다면 어떻게 해결해야 할까요?)
4. 다시 `score`를 출력합니다.

**수행 결과**:
- [x] 가변 변수(`mut`) 이해 및 적용
- [x] 출력 확인 성공

### 문제 3: 상수와 변수 함께 쓰기
**과제**: 
1. 전역 범위(main 함수 밖) 혹은 함수 내에 `BONUS`라는 상수를 선언하고 값으로 50을 줍니다. (타입은 `i32`로 지정하세요)
2. `score` 변수와 `BONUS` 상수를 더해 `total_score`라는 새로운 변수에 담고 출력해 보세요.

**수행 결과**:
- [x] 상수(`const`) 선언 완료
- [x] 변수와 상수의 연산 성공

---

## 🗓️ Day 3: Shadowing (섀도잉)
### 문제 4: Shadowing을 이용한 타입 변환
**과제**: `study_02` 프로젝트에서 다음 작업을 수행하세요.
1. `spaces`라는 변수에 문자열 `"   "` (공백 3개)를 담습니다.
2. Shadowing을 사용하여 `spaces`라는 같은 이름의 변수에 해당 문자열의 **길이**(숫자)를 담습니다. (힌트: `.len()` 메서드 사용)
3. 최종 `spaces` 값을 출력해 보세요.

**수행 결과**:
- [x] 동일한 이름으로 재선언 성공
- [x] 문자열에서 숫자로 타입 변환 확인
- [x] 출력 결과 확인

---

## 🗓️ Day 4: 스칼라 데이터 타입 (1) - 정수형
### 문제 5: 다양한 정수 타입 선언하기
**과제**: `study_03` 프로젝트에서 다음 변수들을 선언하고 출력해 보세요.
1. `age`: 본인의 나이를 적절한 타입으로 담으세요. (음수가 될 수 없고 255를 넘지 않는다면 어떤 타입이 좋을까요?)
2. `salary`: 가상의 월급을 `i32` 타입으로 담으세요.
3. `large_number`: 매우 큰 숫자(예: 10억)를 `i64` 타입으로 담으세요.

**수행 결과**:
- [x] `u8` 타입 활용 성공
- [x] `i32` 타입 활용 성공
- [x] `i64` 타입 활용 성공
- [x] 모든 변수 출력 확인

### 문제 6: 정수 오버플로 관찰 (선택 사항)
**과제**: `u8` 변수에 255를 넣고 1을 더했을 때 어떤 일이 발생하는지 `cargo run`으로 확인해 보세요.

---

## 🗓️ Day 5: 스칼라 데이터 타입 (2) - 소수점, 불리언, 문자
### 문제 7: 다양한 타입 믹스하기
**과제**: `study_04` 프로젝트에서 다음 변수들을 선언하고 출력해 보세요.
1. `pi`: 원주율 3.14159를 `f32` 타입으로 담으세요.
2. `is_rust_fun`: `true` 값을 담는 불리언 변수를 만드세요.
3. `heart_emoji`: 하트 모양 아이콘(💖) 또는 좋아하는 문자 하나를 `char` 타입으로 담으세요.

**수행 결과**:
- [x] `f32` (부동 소수점) 사용 성공
- [x] `bool` (논리형) 사용 성공
- [x] `char` (문자형) 사용 성공
- [x] 모든 변수 출력 확인
